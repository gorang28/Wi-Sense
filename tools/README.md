# Deprecated

This directory is leftover from the old layout. Python code moved to `../python/`, data to `../data/`.

If `venv/` still exists here, recreate the virtualenv at the project root:

```shell
python -m venv venv
pip install -r requirements.txt
```

Then you can delete this `tools/` folder.
